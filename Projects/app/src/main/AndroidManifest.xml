<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stigma_mm1.boostravel">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoosTravel">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/Theme.BoosTravel.NoActionBar" />
        <activity android:name=".mitra_list_jurusan" />
        <activity android:name=".user_side.CariJurusanActivity" />
        <activity android:name=".mitra_lihat_feedback" />
        <activity android:name=".mitra_riwayat_pesanan" />
        <activity android:name=".mitra_side.DashboardMitraActivity" />
        <activity android:name=".detail_travel_rute" />
        <activity android:name=".detail_travel_fitur" />
        <activity android:name=".PengumumanActivity" />
        <activity android:name=".inspirasi_liburan" />
        <activity android:name=".user_side.PesananActivity" />
        <activity android:name=".user_side.FeedbackActivity" />
        <activity android:name=".ListTravelActivity" />
        <activity android:name=".mitra_side.TambahJurusanActivity" />
        <activity android:name=".user_side.DashboardUserActivity" />
        <activity android:name=".welcome" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DaftarActivity" />
        <activity android:name=".mitra_side.DaftarMitraActivity" />
        <activity android:name=".user_side.DaftarUserActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>